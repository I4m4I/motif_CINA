# Training dependencies for the motif-regularized PPO code (main.py / tools.py).
#
# The code uses the LEGACY Gym API:
#   env.seed(seed); obs = env.reset(); obs, r, done, info = env.step(a)
# and classic MuJoCo "-v2" task ids (Ant-v2, Walker2d-v2, InvertedPendulum-v2,
# InvertedDoublePendulum-v2). These require an old gym + mujoco-py stack.
#
# Pinning that reproduces the original runs:
torch>=1.12
numpy
tqdm
gym==0.21.0
mujoco-py<2.2,>=2.1
# mujoco-py additionally needs a local MuJoCo 2.1 binary install and a working
# C compiler. On newer setups gym==0.21 needs:  pip install "setuptools<66" wheel==0.38.4
#
# Plotting (see ../plot) only needs:
matplotlib
